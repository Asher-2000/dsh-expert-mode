/**
 * Expert Mode Pro UI — host-side entry (node).
 * All UI logic lives in client.js (browser bundle).
 * This module is a no-op apply so cordis can register the plugin.
 */

const name = 'dsh-expert-mode-pro-ui';

function apply() {}

export { apply, name };
